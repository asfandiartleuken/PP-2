# Moving Ball (Practice 9)

Interactive red ball that moves with arrow keys on a white canvas.

## Controls
- Arrow keys: move 20px per press
- Q or Esc: quit

Ball movement is clamped so it never leaves the window.

## Running
```
python main.py
```
Ensure `pygame` is installed (see repo-level `requirements.txt`).
